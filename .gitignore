.DS_Store
*.swp
node_modules
mcp-shell.log
bun.lock
bin/links.ts

# AI tools
.claude/settings.local.json
.aider*

# iTerm runtime files (it creates ~/.config/iterm2 = config.ln/iterm2 on launch)
config.ln/iterm2/

# excalidraw skill renderer runtime (created by uv sync on first render)
claude/skills/excalidraw.ln/references/.venv/
claude/skills/excalidraw.ln/references/uv.lock

# b44sync config: the real file lives in ~/projects/apper/susskind, symlinked in
# here by that repo's links.toml. Company tooling, not dotfiles.
config.ln/b44sync/
