font_size 20

# Tabs on top
tab_bar_edge top

# Quit without confirmation even when processes are running
confirm_os_window_close 0

# Auto-start tmux (for Claude Code agent teams)
shell /bin/zsh -l -c tmux
