#!/bin/zsh

# Navigation Aliases
alias jd='cd $DOTFILES'
alias jp='cd ~/projects'

# Global Aliases
alias -g G='| grep'

# ls (BSD/macOS): colorized output + classify + one entry per line
alias ls='ls -GF1'
alias la='ls -lAh'

alias vim='nvim'
